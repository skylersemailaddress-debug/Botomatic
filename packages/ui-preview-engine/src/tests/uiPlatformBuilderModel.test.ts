import assert from "assert"; import { UI_PLATFORM_BUILDER_CAVEAT } from "../uiPlatformBuilderModel";
assert(UI_PLATFORM_BUILDER_CAVEAT.includes("deterministic dry-run planning")); console.log("uiPlatformBuilderModel.test.ts passed");
