[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run ui:build"]

[start]
cmd = "npx tsx --no-warnings apps/orchestrator-api/src/bootstrap.ts"
